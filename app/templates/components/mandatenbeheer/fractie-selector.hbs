<Mandatenbeheer::PowerSelect
        @searchEnabled={{true}}
        @allowClear={{allowClear}}
        @disabled={{disabled}}
        @placeholder="Zoek fractie"
        @search={{perform searchByName}}
        @selected={{_fractie}}
        @onChange={{action "select"}}
        as |fractie|>
  {{fractie.naam}}
</Mandatenbeheer::PowerSelect>
