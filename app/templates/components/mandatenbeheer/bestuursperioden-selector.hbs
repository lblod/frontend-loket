{{#power-select
    options=options
    selected=selectedBestuursorgaan
    searchEnabled=false
    onchange=(action "selectBestuursorgaan")
    as |orgaan|}}
  Bestuursperiode {{moment-format orgaan.bindingStart 'YYYY'}}
  -
  {{#if orgaan.bindingEinde}}
    {{moment-format orgaan.bindingEinde 'YYYY'}}
  {{else}}
    heden
  {{/if}}
{{/power-select}}