<a class="small link--dark--light toggle toggle--arrow-down js-accordion__toggle js-accordion-bound" {{action "toggleOpen"}}>
  <i class="toggle__icon" aria-hidden="true"></i>Toon / verberg totalen</a>
<div class="accordion__content u-spacer--top--tiny u-bg-block">
  {{#if getMandatarissenTotals.isRunning}}
    <div class="loader-wrapper">
      <div class="loader">
        <span class="u-visually-hidden">Aan het laden</span>
      </div>
    </div>
  {{else}}
    {{#each mandatarissenTotals as |orgaanMap|}}
      Bestuursorgaan: {{orgaanMap.orgaan.isTijdsspecialisatieVan.naam}}: van {{moment-format orgaanMap.orgaan.bindingStart "YYYY"}} tot {{#if orgaanMap.orgaan.bindingEinde}}{{moment-format orgaanMap.orgaan.bindingEinde "YYYY"}}{{else}}heden{{/if}}
      <ul class="inline-list inline-list--bordered">
        {{#each orgaanMap.mandaten as |total|}}
          <li>
            {{total.naam}}: {{total.aantal}}
          </li>
        {{/each}}
      </ul>
   {{/each}}
  {{/if}}
</div>
