{{#power-select
   allowClear=allowClear
   disabled=disabled
   selected=_address
   options=options
   onchange=(action "select")
   loadingMessage="Aan het laden..."
   noMatchesMessage="Geen resultaten"
   as |address|}}
  {{address.volledigAdres.geografischeNaam.spelling}}
{{/power-select}}
