{{page-title "Bekijk"}}

<AuToolbar @border="bottom" @size="large" as |Group|>
  <Group>
    <div>
      <AuHeading @skin="2">Bekijk mandataris</AuHeading>
      <p class="au-u-h3">
        {{@model.isBestuurlijkeAliasVan.gebruikteVoornaam}}
        {{@model.isBestuurlijkeAliasVan.achternaam~}}

        {{~#if @model.bekleedt~}}
          ,
          {{@model.bekleedt.bestuursfunctie.label}}
        {{/if}}
      </p>
    </div>
  </Group>

</AuToolbar>

<AuBodyContainer @scroll={{true}}>
  {{#if this.bestuurseenheid.hasEditRight}}
    <AuAlert @icon="info-circle" @skin="info" @size="small">Deze mandatarisgegevens zijn alleen leesbaar. Om ze te bewerken klikt u op <AuLink @route="eredienst-mandatenbeheer.mandataris.edit">bewerken</AuLink>.</AuAlert>
  {{else}}
    <AuAlert @icon="alert-triangle" @skin="warning" @size="small">Deze mandatarisgegevens zijn alleen leesbaar. Je kunt ze niet bewerken.</AuAlert>
  {{/if}}
  <dl class="au-u-2-3@medium au-o-box">
    <div class="au-u-margin-bottom-small">
      <dt class="au-c-label">
        Mandaat
      </dt>
      <dd>
        {{@model.bekleedt.bestuursfunctie.label}}
      </dd>
    </div>
    <div class="au-o-grid au-o-grid--tiny au-u-margin-bottom-small">
      <div class="au-o-grid__item au-u-1-2">
        <dt class="au-c-label">
          Startdatum
        </dt>
        <dd>
          {{moment-format @model.start "DD/MM/YYYY"}}
        </dd>
      </div>
      <div class="au-o-grid__item au-u-1-2">
        <dt class="au-c-label">Einddatum</dt>
        <dd>
          <ValueWithPlaceholder @value={{@model.einde}}>
            <:value as |value|>
              {{moment-format value "DD/MM/YYYY"}}
            </:value>
          </ValueWithPlaceholder>
        </dd>
      </div>
    </div>
    {{#if @model.bekleedt}}
      <div class="au-u-margin-bottom-small">
        <dt class="au-c-label">Geplande einddatum</dt>
        <dd>
          <ValueWithPlaceholder @value={{@model.expectedEndDate}}>
            <:value as |value|>
              {{moment-format value "DD/MM/YYYY"}}
            </:value>
          </ValueWithPlaceholder>
        </dd>
      </div>
    {{/if}}
  </dl>

  <AuHr @size="small" />

  {{#if this.contactList}}
    <div class="au-o-box">
      <AuHeading @skin="2" class="au-u-h3 au-u-padding-bottom">
        Contactgegevens
      </AuHeading>
      {{#each this.contactList as |contact|}}
        <dl class="au-o-grid au-o-grid--tiny au-u-margin-bottom-small">
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">Adres</dt>
            <dd>{{contact.adres.volledigAdres}}</dd>
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">E-mail</dt>
            <dd>{{contact.email}}</dd>
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">Primair telefoonnummer</dt>
            <dd>{{contact.telefoon}}</dd>
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">Secondair telefoonnummer</dt>
            <dd>
              <ValueWithPlaceholder
                @value={{contact.secondaryContactPoint.telefoon}}
              />
            </dd>
          </div>
        </dl>
        <AuHr @size="small" />
      {{/each}}
    </div>
  {{/if}}
</AuBodyContainer>