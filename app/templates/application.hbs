<div class="container">
  <div class="temporary-header">
    <div class="grid">
      <div class="col--9-12 col--1-1-s">
        {{#link-to 'index' class="temporary-header__logo"}}{{/link-to}}
        <div class="temporary-header__vlaanderen">Vlaanderen</div>
        <div class="temporary-header__name u-hidden-mobile">
          <p>loket.lokaalbestuur.vlaanderen.be</p>
        </div>
      </div>
      <div class="col--3-12 u-align-right u-hidden-mobile">
        <div class="temporary-header__info">
          <p>Demo-omgeving</p>
        </div>
      </div>
    </div>
  </div>
  {{#if session.isAuthenticated}}
  <div class="container-flex--contain">
    <div class="grid grid--collapse">
      <div class="col--2-12 col--3-12--m u-hidden--s">
        <div class="container-flex--contain">

            <div class="sidebar">
              <div class="sidebar__content">
                <nav class="side-navigation">
                  <div class="side-navigation__content">
                    <ul class="side-navigation__group">
                      <li class="side-navigation__item">
                        <a href="/loket/loket-inbox.html">Inbox</a>
                      </li>
                      <li class="side-navigation__item">
                        {{#link-to "mandatenbeheer.mandatarissen.index"}}Mandatenbeheer{{/link-to}}
                      </li>
                      <li class="side-navigation__item">
                        <a href="/loket/loket-bbcdr.html">BBC DR</a>
                      </li>
                      {{#if session.isAuthenticated}}
                        <li>{{mock-logout}}</li>
                      {{/if}}
                    </ul>
                  </div>
                </nav>
              </div>
              <div class="sidebar__footer">
                <button class="button button--block button--alt">Persoonlijke gegevens</button>
              </div>
            </div>
        </div>
      </div>
      {{outlet}}
    </div>
  </div>
  {{else}}
    {{outlet}}
  {{/if}}
</div>
