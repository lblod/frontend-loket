{{head-layout}}
<div class="container">
  {{wu-widget src=header class="vo-header-widget"}}
  {{#if session.isAuthenticated}}
    <div class="container-flex--contain">
      <div class="grid grid--collapse">
        <div class="col--2-12 col--3-12--m u-hidden--s">
          <div class="container-flex--contain">
            <div class="sidebar">
              <div class="sidebar__header">
								<div class="user-toggle">
								  <nav class="text-left u-align-left">
                    {{#wu-popover as |p|}}
                      {{#p.toggle}}
			                  <div class="user-toggle__name--label">{{currentSession._user.voornaam}} {{currentSession._user.achternaam}}</div>
                        <div class="user-toggle__name">
                          {{currentSession._group.classificatie.label}} {{currentSession._group.naam}}
                        </div>
                        <button class="vi vi-arrow vi-u-90deg vi-u-badge vi-u-badge--xsmall vi-u-badge--neutral">
                          <span class="u-hidden-visually">Account settings</span>
                        </button>
                      {{/p.toggle}}
                      {{#p.content tagName="ul"}}
                          <li>
                            {{#acmidm-switch tagName="a" class="popover__link"}}
	                            <i class="fa fa-retweet button__icon"></i>
	                            <span>Wissel van bestuurseenheid</span>
                            {{/acmidm-switch}}
                          </li>
                          <li>
                            {{#acmidm-logout tagName="a" class="popover__link"}}
	                            <i class="button__icon fa fa-power-off"></i>
                            	<span>Meld u af</span>
                            {{/acmidm-logout}}
                          </li>
                      {{/p.content}}
                    {{/wu-popover}}
								  </nav>
								</div>
              </div>
              <div class="sidebar__content">
                <nav class="side-navigation">
                  <div class="side-navigation__content">
                    <ul class="side-navigation__group">
                      {{#if currentSession.canAccessToezicht}}
                        <li class="side-navigation__item">
                          {{#link-to "toezicht.inzendingen"}}Toezicht{{/link-to}}
                        </li>
                      {{/if}}
                      {{#if currentSession.canAccessBerichten}}
                        <li class="side-navigation__item">
                          {{#link-to "berichtencentrum.berichten"}}Berichtencentrum{{/link-to}}
                        </li>
                      {{/if}}
                      {{#if currentSession.canAccessBbcdr}}
                        <li class="side-navigation__item">
                          {{#link-to "bbcdr.rapporten"}}BBC DR{{/link-to}}
                        </li>
                      {{/if}}
                      {{#if currentSession.canAccessMandaat}}
                        <li class="side-navigation__item">
                          {{#link-to "mandatenbeheer.mandatarissen"}}Mandatenbeheer{{/link-to}}
                        </li>
                      {{/if}}
                      {{#if currentSession.canAccessLeidinggevenden}}
                        <li class="side-navigation__item">
                          {{#link-to "leidinggevendenbeheer.bestuursfuncties"}}Leidinggevendenbeheer{{/link-to}}
                        </li>
                      {{/if}}
                      {{#if currentSession.canAccessPersoneelsbeheer}}
                        <li class="side-navigation__item">
                        {{#link-to "personeelsbeheer.personeelsaantallen.index"}}Personeelsbeheer{{/link-to}}
                        </li>
                      {{/if}}
                    </ul>
                  </div>
                </nav>
              </div>
              <div class="sidebar__footer u-align-center">
                <a href="https://loket.lokaalbestuur.vlaanderen.be/handleiding/" target="_blank" class="button button--alt button--small button--block u-spacer--tb--tiny"><i class="vi vi-book vi-u-s"></i> Bekijk handleiding</a>
              </div>
            </div>
          </div>
        </div>
        {{outlet}}
      </div>
    </div>
  {{else}}
    {{outlet}}
  {{/if}}
</div>
