{{ page-title "Voeg een nieuwe persoon toe"}}

<AuToolbar @size="large" @border="bottom" as |Group|>
  <Group>
    <AuHeading @skin="2">Voeg een nieuwe persoon toe</AuHeading>
  </Group>
</AuToolbar>

<Shared::Persoon::CreatePersoon
  @prefilledValues={{this.personPrefilledValues}}
  @nationalityRequired={{true}}
  @geboorteRequired={{true}}
  @onCreate={{this.onCreate}}
  @onCancel={{this.onCancel}}
/>