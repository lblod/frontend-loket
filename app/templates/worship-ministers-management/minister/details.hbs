{{#let @model.ministerPosition as |hasMinisterPosition|}}
  {{page-title
    "Bekijk bedienaar "
    @model.person.gebruikteVoornaam
    " "
    @model.person.achternaam
    (if
      hasMinisterPosition
      (concat ", " @model.ministerPosition.function.label)
    )
  }}

  <AuToolbar @border="bottom" @size="large" as |Group|>
    <Group>
      <div>
        <AuHeading @skin="2">Bekijk bedienaar</AuHeading>
        <p class="au-u-h3">
          {{@model.person.gebruikteVoornaam}}
          {{@model.person.achternaam~}}
          {{#if hasMinisterPosition~}}
            ,
            {{@model.ministerPosition.function.label}}
          {{/if}}
        </p>
      </div>
    </Group>
  </AuToolbar>
{{/let}}

<AuBodyContainer @scroll={{true}}>
  {{#if this.bestuurseenheid.hasEditRight}}
    <AuAlert @icon="info-circle" @skin="info" @size="small">Deze bedienaargegevens zijn alleen leesbaar. Om ze te bewerken klikt u op <AuLink @route="worship-ministers-management.minister.edit">bewerken</AuLink>.</AuAlert>
  {{else}}
    <AuAlert @icon="alert-triangle" @skin="warning" @size="small">Deze mandatarisgegevens zijn alleen leesbaar. Je kunt ze niet bewerken want ze worden beheerd door uw softwareleverancier.</AuAlert>
  {{/if}}
  <dl class="au-u-2-3@medium au-o-box">
    <div class="au-u-margin-bottom-small">
      <dt class="au-c-label">Functienaam</dt>
      <dd>
        {{@model.ministerPosition.function.label}}
      </dd>
    </div>
    <div class="au-o-grid au-o-grid--tiny au-u-margin-bottom-small">
      <div class="au-o-grid__item au-u-1-2">
        <dt class="au-c-label">Startdatum</dt>
        <dd>
          {{moment-format @model.agentStartDate "DD/MM/YYYY"}}
        </dd>
      </div>
      <div class="au-o-grid__item au-u-1-2">
        <dt class="au-c-label">Einddatum</dt>
        <dd>
          <ValueWithPlaceholder @value={{@model.agentEndDate}}>
            <:value as |value|>
              {{moment-format value "DD/MM/YYYY"}}
            </:value>
          </ValueWithPlaceholder>
        </dd>
      </div>
    </div>
  </dl>

  <AuHr @size="small" />

  {{#if @model.contacts}}
    <div class="au-o-box">
      <AuHeading @skin="2" class="au-u-padding-bottom">
        Contactgegevens
      </AuHeading>
      {{#each @model.contacts as |contact|}}
        <dl class="au-o-grid au-o-grid--tiny au-u-margin-bottom-small">
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">Adres</dt>
            <dd>{{contact.adres.volledigAdres}}</dd>
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">E-mail</dt>
            <dd>{{contact.email}}</dd>
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">Primair telefoonnummer</dt>
            <dd>{{contact.telefoon}}</dd>
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <dt class="au-c-label">Secondair telefoonnummer</dt>
            <dd>
              <ValueWithPlaceholder @value={{contact.secondoryContactPoint.telefoon}} />
            </dd>
          </div>

        </dl>
        <AuHr @size="small" />
      {{/each}}
    </div>
  {{/if}}
</AuBodyContainer>