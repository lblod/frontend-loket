{{#if @website}}
  <AuLinkExternal
    href={{remap-url @website.url}}
    @skin="button"
    @icon="external-link"
    @iconAlignment="right"
    class="au-u-margin-bottom-small">
    {{@website.title.default}}
  </AuLinkExternal>
{{/if}}
