<DataCard>
  <:title>
    {{yield to="title"}}
  </:title>
  <:card as |Card|>
    <Card.Columns>
      <:left as |Item|>
        <Item>
          <:label>Adres</:label>
          <:content>
            {{@address}}
          </:content>
        </Item>
        {{#if @province}}
          <Item>
            <:label>Provincie</:label>
            <:content>
              {{@province}}
            </:content>
          </Item>
        {{/if}}
      </:left>
      <:right as |Item|>
        {{#if (eq @type "primary")}}
          <Item>
            <:label>Primair telefoonnummer</:label>
            <:content>
              {{@primaryContact.telephone}}
            </:content>
          </Item>
        {{/if}}
        {{#if (eq @type "secondary")}}
          <Item>
            <:label>Secundair telefoonnummer</:label>
            <:content>
              {{@secondaryContact.telephone}}
            </:content>
          </Item>
        {{/if}}
        {{#if @primaryContact.email}}
          <Item>
            <:label>E-mail</:label>
            <:content>
              {{@primaryContact.email}}
            </:content>
          </Item>
        {{/if}}
        {{#if @primaryContact.website}}
          <Item>
            <:label>Website</:label>
            <:content>
              <AuLinkExternal href={{@primaryContact.website}}>
                {{@primaryContact.website}}
                (externe link)
              </AuLinkExternal>
            </:content>
          </Item>
        {{/if}}
      </:right>
    </Card.Columns>
  </:card>
</DataCard>