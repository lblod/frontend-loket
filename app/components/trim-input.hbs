<AuInput
  @disabled={{@disabled}}
  @error={{@error}}
  autocomplete={{@autocomplete}}
  @width={{@width}}
  @id={{@id}}
  @value={{@value}}
  {{on "focusout" (perform this.trimInput)}}
  ...attributes
/>