<AuLink
  @route="auth.login" {{! TODO: it might be nice if this would link to the mock-login route based on some environment variable}}
  @skin={{if @isCompact "primary" "button"}}
  @icon="login"
>
  Aanmelden
</AuLink>