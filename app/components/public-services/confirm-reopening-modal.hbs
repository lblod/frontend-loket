<AuModal
  @modalOpen={{true}}
  @closeModal={{this.close}}
  data-test-confirm-reopening-modal="modal"
>
  <:title>Product of dienst opnieuw bewerken?</:title>
  <:body>
    {{! TODO: add proper copy }}
    Om wijzigingen aan te brengen moet het product eerst naar de ontwerp status
    worden gebracht. Nieuwe wijzigingen zullen daarna opnieuw gepubliceerd
    moeten worden.
  </:body>
  <:footer>
    <AuButtonGroup>
      <AuButton
        @icon="pencil"
        @disabled={{this.reopen.isRunning}}
        @loading={{this.reopen.isRunning}}
        {{on "click" (perform this.reopen)}}
        data-test-confirm-reopening-modal="confirm-button"
      >
        Product opnieuw bewerken
      </AuButton>
      <AuButton
        @skin="secondary"
        @disabled={{this.reopen.isRunning}}
        {{on "click" this.close}}
        data-test-confirm-reopening-modal="cancel-button"
      >
        Annuleer
      </AuButton>
    </AuButtonGroup>
  </:footer>
</AuModal>