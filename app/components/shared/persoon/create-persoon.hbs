<div class="au-c-body-container au-c-body-container--scroll">
  <div class="au-o-box">
    <div class="au-c-form">
      <div>
        <div class="au-o-grid au-o-grid--tiny">
          <div class="au-o-grid__item au-u-1-2">
            <AuLabel @error="{{if this.voornaamError 'true'}}" for="mandataris-naam">Voornaam</AuLabel>
            {{input class=(with-error-class this.voornaamError "au-c-input au-c-input--block js-js-input-pattern-bound") id="mandataris-naam" name="mandataris-naam" value=(mut this.voornaam)}}
            {{#if this.voornaamError}}
              <AuHelpText @error="true">{{this.voornaamError}}</AuHelpText>
            {{/if}}
          </div>
          <div class="au-o-grid__item au-u-1-2">
            <AuLabel @error="{{if this.familienaamError 'true'}}" for="mandataris-familienaam">Familienaam</AuLabel>
            {{input class=(with-error-class this.familienaamError "au-c-input au-c-input--block js-js-input-pattern-bound") id="mandataris-familienaam" name="mandataris-familienaam" value=(mut this.familienaam)}}
            {{#if this.familienaamError}}
              <AuHelpText @error="true">{{this.familienaamError}}</AuHelpText>
            {{/if}}
          </div>
        </div>
      </div>
      <div>
        <AuLabel for="mandataris-roepnaam">Roepnaam</AuLabel>
        {{input class="au-c-input au-c-input--block js-js-input-pattern-bound" id="mandataris-roepnaam" name="mandataris-roepnaam" placeholder="Jos" value=(mut this.roepnaam)}}
      </div>
      <div>
        {{#let (if this.rijksregisternummerError true false) as |showError|}}
          <AuLabel @error={{showError}} for="mandataris-rijksregister">Rijksregisternummer</AuLabel>
          <AuInput
            @error={{showError}}
            @mask="99.99.99-999.99"
            @maskPlaceholder="_"
            @width="block"
            @onChange={{fn (mut this.rijksregisternummer)}}
            value={{this.rijksregisternummer}}
            id="mandataris-rijksregister"
            placeholder="00.00.00-000.00"
          />
          {{#if showError}}
            <AuHelpText @error="true">{{this.rijksregisternummerError}}</AuHelpText>
          {{/if}}
        {{/let}}
      </div>
      <div>
        <AuLabel for="mandataris-geboorte">Geboortedatum</AuLabel>
        <AuDatePicker
          @value={{this.birthDate}}
          @min={{this.minDate}}
          @max={{this.maxDate}}
          @id="mandataris-geboorte"
          @onChange={{action "setDateOfBirth"}}
        />
      </div>
      <div>
        <AuLabel @error="{{if this.geslachtError 'true'}}">Geslacht</AuLabel>
        <div class="au-o-grid au-o-grid--tiny">
          <div class="au-o-grid__item au-u-1-2 au-u-1-4@medium">
            <label class="au-c-control au-c-control--radio">
              <input class="au-c-control__input" name="radio-buttons" onclick={{action 'setGender'}} value="{{this.female}}" checked={{this.isFemale}} type="radio">
              <span class="au-c-control__indicator"></span>Vrouw
            </label>
          </div>
          <div class="au-o-grid__item au-u-1-2 au-u-1-4@medium">
            <label class="au-c-control au-c-control--radio">
              <input class="au-c-control__input" name="radio-buttons" onclick={{action 'setGender'}} value={{this.male}} checked={{this.isMale}} type="radio">
              <span class="au-c-control__indicator"></span>Man
            </label>
          </div>
          {{#if this.geslachtError}}
            <AuHelpText @error="true">{{this.geslachtError}}</AuHelpText>
          {{/if}}
        </div>
      </div>
      {{#if this.saveError}}
        <AuAlert @alertIcon="alert-triangle" @alertTitle="Fout bij opslaan" @alertskin="error" @alertsize="small">
          <p>{{this.saveError}}</p>
        </AuAlert>
      {{/if}}
    </div>
  </div>
</div>
<AuToolbar @border="top" @size="large">
  <AuToolbarGroup>
    <AuButton {{on "click" (perform this.save)}} @disabled={{this.save.isRunning}} @loading={{this.save.isRunning}} class="show-mandate-edit-new-next">Voeg persoon toe</AuButton>
    <AuButton {{on "click" (action this.onCancel)}} @skin="secondary" class="toggle-person-new">Annuleer</AuButton>
  </AuToolbarGroup>
</AuToolbar>
