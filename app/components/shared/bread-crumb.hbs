{{#each this.crumbsForRoute as |crumb|}}
  <li class="au-c-list-horizontal__item">
    {{#if crumb.link}}
      <AuLink @linkRoute={{crumb.link}} role="menuitem">
        {{crumb.label}}
      </AuLink>
    {{else}}
      {{crumb.label}}
    {{/if}}
  </li>
{{/each}}
